<header class="hero is-primary">
	<div class="hero-head">
		{{navigation}}
	</div>
	<div class="hero-body">
		<div class="container has-text-centered">
			<h1 class="title is-1 is-spaced">
				{{#if @blog.logo}}<img src="{{@blog.logo}}" class="inhabitu-logo" alt="{{@blog.title}}">{{else}}{{@blog.title}}{{/if}}
			</h1>
			<p class="title is-3">{{@blog.description}}</p>
		</div>
	</div>
</header>
